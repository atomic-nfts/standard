"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
exports.Update = void 0;
var Update = function (state, action) {
    var caller = action.caller;
    var input = action.input;
    var settings = input.settings || state.settings;
    var config = input.config || state.config;
    ContractAssert(state.settings.admins.includes(caller), "Only admins can invoke this function.");
    return __assign(__assign({}, state), { settings: settings, config: config });
};
exports.Update = Update;
