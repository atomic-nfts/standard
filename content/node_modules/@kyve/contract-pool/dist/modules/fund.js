"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
exports.__esModule = true;
exports.Fund = void 0;
var Fund = function (state, action) {
    var credit = state.credit;
    var caller = action.caller;
    var input = action.input;
    var qty = input.qty;
    ContractAssert(caller in credit, "Caller has no balance in the pool.");
    ContractAssert(credit[caller].amount >= qty, "Caller can't fund more than they have.");
    credit[caller].amount -= qty;
    credit[caller].fund += qty;
    return __assign(__assign({}, state), { credit: credit });
};
exports.Fund = Fund;
