import KYVE from "./index";
import { UploadFunction, ValidateFunction } from "./faces";
import Arweave from "arweave";
declare class TestInstance extends KYVE {
    private readonly isUploader;
    constructor(options: {
        pool: {
            id: string;
            pool: any;
        };
        uploader: boolean;
        arweave?: Arweave;
    }, uploadFunc: UploadFunction, validateFunc: ValidateFunction);
    run(): Promise<void>;
    validateTx(id: string): Promise<void>;
}
export default TestInstance;
