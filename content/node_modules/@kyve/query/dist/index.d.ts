import ArDB from "ardb";
import Arweave from "arweave";
export { interactRead, readContract } from "./smartweave";
export declare class Query extends ArDB {
    private poolID;
    deRef: boolean;
    constructor(poolID: string, deRef?: boolean, arweave?: Arweave);
    find(): Promise<any[]>;
    next(): Promise<any[]>;
}
